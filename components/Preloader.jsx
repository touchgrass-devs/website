// UNUSED — safe to delete.
//
// Was the full-screen "words" preloader (cycling greetings + curved wipe
// exit + shared brand-mark hand-off into Hero's headline) that ran on every
// page load. Removed on request ("reset it to how it was before adding the
// preloader, just remove it") - app/layout.js no longer renders this or
// wraps the app in LoadingProvider.
//
// Left as a stub (not deleted) because this sandbox's file mount doesn't
// reliably support deletes — please delete this file manually, along with
// components/LoadingProvider.jsx (same reason, same request).
