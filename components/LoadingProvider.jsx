// UNUSED — safe to delete.
//
// Was the context provider (isLoading / finishLoading, body-scroll + Lenis
// lock) that Preloader.jsx and Hero.jsx gated on. Removed on request
// ("reset it to how it was before adding the preloader, just remove it") -
// app/layout.js no longer wraps the app in this, and Hero.jsx no longer
// imports useLoading.
//
// Left as a stub (not deleted) because this sandbox's file mount doesn't
// reliably support deletes — please delete this file manually, along with
// components/Preloader.jsx (same reason, same request).
