// UNUSED — safe to delete.
//
// This wrapped Hero with the dotted background as a page-level sibling
// (dots behind the whole hero section). That approach was replaced: the dot
// layer now lives inside Hero.jsx itself, stacked directly above the
// section's own white background and below the content layer. See
// components/Hero.jsx.
//
// Left as a stub (not deleted) because this sandbox's file mount doesn't
// reliably support deletes — please delete this file manually.
